.gradle/
build/
*.class
.idea/
!gradle-wrapper.jar
/tmp/
cache-data/
client/
.git-pat

# Generated server RSA keypair (tools:client-patch) — the gateway's private key. Never commit.
/server-rsa.properties
.kotlin/

# Local assistant state is not part of the shippable project. Repository documentation under
# docs/ is source-controlled architecture and protocol ground truth (CLAUDE.md, "Ground-truth
# docs") and must remain trackable.
.claude/
.superpowers/
