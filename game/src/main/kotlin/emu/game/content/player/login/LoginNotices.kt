package emu.game.content.player.login

/** Feature-local login messages, independent of any network revision. */
object LoginNotices {
    val ALL: List<LoginNotice> = listOf(LoginNotice("Welcome to RuneScape."))
}
