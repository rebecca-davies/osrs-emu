package emu.game.content.beta.supplies

/** Runtime policy for the stock beta-world Tournament Supplies interface. */
internal data class TournamentSuppliesConfig(
    val previewInventory: Int,
    val itemCatalogueEnum: Int,
    val itemStackSize: Int,
)
